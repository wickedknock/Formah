const x = 1
var y = "hello world"
const fn = (a,b,c) => {
  if(a){
    return a+b+c
  }
  return null
}

const obj = {foo: 'bar', baz: 'qux', another: 'value', more: 'stuff', extra: 'things', last: 'one'}

function greet(name,greeting,punctuation){
return greeting+', '+name+punctuation
}
